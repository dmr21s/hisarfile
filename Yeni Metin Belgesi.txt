@app.before_request
def show_environ():
    print("==== HEADERS ====")
    for k, v in request.headers.items():
        print(k, ":", v)
    print("==== ENVIRON ====")
    for k, v in request.environ.items():
        if "USER" in k or "AUTH" in k:
            print(k, ":", v)
